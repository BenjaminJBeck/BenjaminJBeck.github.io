<style>
html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  width: 100%;
  overflow: hidden;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

#left-box {
  position: fixed;
  top: 0;
  left: 0;
  width: 170px;
  height: calc(100vh - 100px); /* Subtract bottom bar height */
  background: linear-gradient(180deg, #2c3e50 0%, #34495e 100%);
  border-right: 1px solid rgba(255, 255, 255, 0.1);
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  padding-top: 0px;
  z-index: 100; /* Higher than menu but lower than file tab */
  box-shadow: 4px 0 20px rgba(0, 0, 0, 0.15);
  overflow-y: auto;
  backdrop-filter: blur(10px);

  /* Hide scrollbar visually */
  -ms-overflow-style: none;  /* IE/Edge */
  scrollbar-width: none;     /* Firefox */
}

#left-box::-webkit-scrollbar {
  display: none; /* Chrome/Safari/Edge */
}

#page-label {
  width: 100px;
  height: 35px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 8px;
  margin: 15px 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 12px;
  font-weight: 700;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.sidebar-button {
  width: 150px;
  height: 35px;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  border-radius: 8px;
  margin: 3px 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  flex-shrink: 0;
  border: 1px solid rgba(255, 255, 255, 0.1);
  position: relative;
  overflow: hidden;
  color: rgba(255, 255, 255, 0.9);
  font-size: 11px;
  font-weight: 600;
}

.sidebar-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s;
}

.sidebar-button:hover::before {
  left: 100%;
}

.sidebar-button:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
  color: white;
}

.sidebar-button:active {
  transform: translateY(0px);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}

#bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100px;
  background: linear-gradient(180deg, #2c3e50 0%, #34495e 100%);
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.15);
  box-sizing: border-box;
  display: flex;
  align-items: center;
  padding-left: 130px;
  padding-right: 120px; /* Enough space to avoid trash can */
  z-index: 2;
  overflow: hidden; /* Hide scrollbars */
  backdrop-filter: blur(10px);
}

#page-buttons {
  display: flex;
  align-items: center;
  overflow-x: auto;
  overflow-y: hidden;
  padding-right: 180px; /* Prevents pages from going under trash can */
  scrollbar-width: none; /* Firefox */
  -ms-overflow-style: none; /* IE/Edge */
  gap: 8px;
}
#page-buttons::-webkit-scrollbar {
  display: none; /* Chrome/Safari */
}

.page-button {
  min-width: 65px;
  height: 65px;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  border-radius: 14px;
  margin: 0 2px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  border: 1px solid rgba(255, 255, 255, 0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  user-select: none;
  /* Enable dragging for page buttons specifically */
  -webkit-user-drag: element;
  /* Allow pointer events for dragging */
  pointer-events: auto;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  position: relative;
  overflow: hidden;
}

.page-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
  transition: left 0.5s;
}

.page-button:hover::before {
  left: 100%;
}

.page-button:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
}

.page-button.selected {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-weight: 700;
  box-shadow: 0 0 25px rgba(102, 126, 234, 0.6);
  border: 2px solid rgba(255, 255, 255, 0.3);
  transform: scale(1.05);
}

.page-button.dragging {
  cursor: grabbing;
  opacity: 0.8;
  transform: scale(1.1) rotate(2deg);
  z-index: 1000;
  /* Maintain dragging capability during drag state */
  -webkit-user-drag: element;
  pointer-events: auto;
}

#trash-can {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 65px;
  height: 65px;
  background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
  border-radius: 14px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 26px;
  font-weight: bold;
  cursor: pointer;
  border: 1px solid rgba(255, 255, 255, 0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 10;
  box-shadow: 0 4px 15px rgba(231, 76, 60, 0.3);
}

#trash-can:hover {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
  transform: scale(1.05);
  box-shadow: 0 8px 25px rgba(231, 76, 60, 0.5);
}

#trash-can.drag-over {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
  border: 2px solid #fff;
  animation: trashPulse 0.6s infinite;
  box-shadow: 0 0 30px rgba(255, 107, 107, 0.8);
}

@keyframes trashPulse {
  0% { transform: scale(1.05); }
  50% { transform: scale(1.15); }
  100% { transform: scale(1.05); }
}

.page-button.to-delete {
  border: 3px dashed #e74c3c !important;
  background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%) !important;
  animation: deleteWarning 1s infinite;
}

@keyframes deleteWarning {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

#corner-box {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 120px;
  height: 100px;
  background: linear-gradient(180deg, #2c3e50 0%, #34495e 100%);
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  box-sizing: border-box;
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 3;
  backdrop-filter: blur(10px);
}

#corner-button {
  width: 100px;
  height: 35px;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  border-radius: 8px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.1);
  position: relative;
  overflow: hidden;
  color: rgba(255, 255, 255, 0.9);
  font-size: 11px;
  font-weight: 600;
}

#corner-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.5s;
}

#corner-button:hover::before {
  left: 100%;
}

#corner-button:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
  color: white;
}

#scene-area {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  display: flex;
}

#canvas-wrapper {
  width: 1365px;
  height: calc(100vh - 100px); /* Full height minus bottom bar */
  transform-origin: 0 0;
  position: fixed;
  top: 0;
  right: 0; /* Move to right side */
  margin-right: 0;
  transition: transform 0.15s cubic-bezier(0.4, 0, 0.2, 1);
}

#actual-canvas {
  width: 100%;
  height: 100%;
  background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
  position: relative;
  border-radius: 0px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
  border: 1px solid rgba(0, 0, 0, 0.05);
  overflow: hidden;
}

.preview-fullscreen #scene-area {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  z-index: 10000;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.preview-fullscreen #canvas-wrapper {
  transform: scale(1);
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  position: relative;
  right: auto;
}

.preview-fullscreen #actual-canvas {
  margin: auto;
  border-radius: 0;
}

.preview-fullscreen #actual-canvas img {
  pointer-events: auto;       /* still clickable */
  user-select: none;          /* can't select */
  cursor: default !important; /* no move/pointer/crosshair */
}

.selectable {
  cursor: pointer;
}

.selectable.selected {
  outline: 2px solid #4a90e2;
  outline-offset: 3px;
  position: relative;
  box-shadow: 0 0 8px rgba(74, 144, 226, 0.6);
  border-radius: 6px;
  transition: box-shadow 0.2s ease, outline-color 0.2s ease;
}

.preview-fullscreen #actual-canvas img {
  pointer-events: auto;
  user-select: none;
  cursor: default !important;
}

.sidebar-button, .page-button, #corner-button, .popup-btn {
  pointer-events: auto !important;
  user-select: auto !important;
}

img.selectable {
  user-select: none;
  -webkit-user-drag: none;
}

#code-popup {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
  padding: 35px;
  border-radius: 20px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  z-index: 3000;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  width: 650px;
  max-width: 95vw;
  max-height: 90vh;
  overflow-y: auto;
  border: 1px solid rgba(0, 0, 0, 0.05);
  backdrop-filter: blur(10px);
}


<div id="image-visuals-popup">
<div class="popup-title">Image Visuals</div>

<div class="visuals-section">
<div class="visuals-grid">
<div class="visual-control">
<label>Brightness</label>
<input type="range" id="brightness" min="0" max="200" value="100">
<span id="brightness-value">100%</span>
</div>

<div class="visual-control">
<label>Contrast</label>
<input type="range" id="contrast" min="0" max="200" value="100">
<span id="contrast-value">100%</span>
</div>

<div class="visual-control">
<label>Saturation</label>
<input type="range" id="saturation" min="0" max="200" value="100">
<span id="saturation-value">100%</span>
</div>

<div class="visual-control">
<label>Hue Rotation</label>
<input type="range" id="hue-rotate" min="0" max="360" value="0">
<span id="hue-rotate-value">0°</span>
</div>

<div class="visual-control">
<label>Grayscale</label>
<input type="range" id="grayscale" min="0" max="100" value="0">
<span id="grayscale-value">0%</span>
</div>

<div class="visual-control">
<label>Sepia</label>
<input type="range" id="sepia" min="0" max="100" value="0">
<span id="sepia-value">0%</span>
</div>

<div class="visual-control">
<label>Invert</label>
<input type="range" id="invert" min="0" max="100" value="0">
<span id="invert-value">0%</span>
</div>

<div class="visual-control">
<label>Blur</label>
<input type="range" id="blur" min="0" max="10" value="0" step="0.1">
<span id="blur-value">0px</span>
</div>

<div class="visual-control">
<label>Opacity</label>
<input type="range" id="opacity" min="0" max="100" value="100">
<span id="opacity-value">100%</span>
</div>

<div class="visual-control">
<label>Tint Color</label>
<input type="color" id="tint-color" value="#ffffff">
<input type="range" id="tint-opacity" min="0" max="100" value="0">
<span id="tint-opacity-value">0%</span>
</div>
</div>
</div>

<div class="tools-section">
<h3 style="margin: 0 0 15px 0; color: #2d3748;">Drawing Tools</h3>
<div class="tools-grid">
<button class="tool-button" data-tool="select">Select</button>
<button class="tool-button" data-tool="glow">Glow Draw</button>
<button class="tool-button" data-tool="shadow">Shadow Draw</button>
<button class="tool-button" data-tool="eraser">Eraser</button>
</div>

<div class="tool-controls">
<div class="visual-control">
<label>Tool Size</label>
<input type="range" id="tool-size" min="1" max="50" value="10">
<span id="tool-size-value">10px</span>
</div>

<div class="visual-control">
<label>Tool Intensity</label>
<input type="range" id="tool-intensity" min="0" max="100" value="50">
<span id="tool-intensity-value">50%</span>
</div>

<div class="visual-control">
<label>Tool Color</label>
<input type="color" id="tool-color" value="#ffffff">
</div>
</div>
</div>

<div class="popup-buttons">
<button class="popup-btn cancel" onclick="closeImageVisualsPopup()">Cancel</button>
<button class="popup-btn" onclick="applyImageVisuals()">Apply</button>
</div>
</div>

#code-popup textarea {
  width: 100%;
  height: 320px;
  border: 1px solid #e2e8f0;
  border-radius: 12px;
  padding: 18px;
  font-family: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', monospace;
  font-size: 14px;
  resize: none;
  background: #f8fafc;
  color: #2d3748;
  transition: all 0.2s ease;
}

#code-popup textarea:focus {
  outline: none;
  border-color: #667eea;
  box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.code-popup-button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 10px;
  padding: 12px 24px;
  font-size: 14px;
  font-weight: 600;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  cursor: pointer;
  transition: all 0.2s ease;
}

.code-popup-button:hover {
  transform: translateY(-1px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
}

#textbox-popup-title {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-size: 20px;
  font-weight: 700;
  padding: 15px 25px;
  border-radius: 12px;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  text-align: center;
  width: 100%;
}

#file-tab {
  width: 80%;
  height: 90px;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 11px;
  font-weight: 700;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.1);
  position: relative;
  z-index: 200; /* Higher than everything else - always on top */
  margin: 0;
  padding: 0;
  margin-top: 10px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border-radius: 8px;
}

#file-tab:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
}

/* NEW SLIDING FILE MENU STYLES */
#file-sliding-menu {
  position: fixed;
  top: 10px;
  left: 34px; /* Start from behind the file button */
  height: 90px;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 8px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  z-index: 50; /* Lower than file tab - stays behind */
  backdrop-filter: blur(10px);
  transform: translateX(0); /* Start at the same position as file button */
  transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), width 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  overflow: hidden;
  display: flex;
  align-items: center;
  width: 136px; /* Same width as file button initially */
}

#file-sliding-menu.open {
  transform: translateX(120px); /* Slide out to the right */
  width: 1005px; /* Expand to fit all items horizontally */
}

.file-menu-item {
  height: 70px;
  min-width: 90px;
  padding: 0 15px;
  text-align: center;
  color: white;
  background: transparent;
  border: none;
  border-right: 1px solid rgba(255, 255, 255, 0.1);
  cursor: pointer;
  font-size: 11px;
  font-weight: 600;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  opacity: 0;
  transform: translateX(-20px);
  position: relative;
}

#file-sliding-menu.open .file-menu-item {
  opacity: 1;
  transform: translateX(0);
}

#file-sliding-menu.open .file-menu-item:nth-child(1) { transition-delay: 0.1s; }
#file-sliding-menu.open .file-menu-item:nth-child(2) { transition-delay: 0.15s; }
#file-sliding-menu.open .file-menu-item:nth-child(3) { transition-delay: 0.2s; }
#file-sliding-menu.open .file-menu-item:nth-child(4) { transition-delay: 0.25s; }
#file-sliding-menu.open .file-menu-item:nth-child(5) { transition-delay: 0.3s; }
#file-sliding-menu.open .file-menu-item:nth-child(6) { transition-delay: 0.35s; }
#file-sliding-menu.open .file-menu-item:nth-child(7) { transition-delay: 0.4s; }
#file-sliding-menu.open .file-menu-item:nth-child(8) { transition-delay: 0.45s; }
#file-sliding-menu.open .file-menu-item:nth-child(9) { transition-delay: 0.5s; }
#file-sliding-menu.open .file-menu-item:nth-child(10) { transition-delay: 0.55s; }
#file-sliding-menu.open .file-menu-item:nth-child(11) { transition-delay: 0.6s; }

.file-menu-item:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateY(-2px);
}

.file-menu-item:last-child {
  border-right: none;
}

/* Overlay to close menu when clicking outside */
#file-menu-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: transparent;
  z-index: 49; /* Lower than menu */
  display: none;
  pointer-events: none; /* Don't interfere with menu interactions */
}

#file-menu-overlay.active {
  display: block;
  pointer-events: auto; /* Only active when menu is open */
}

.file-dropdown-item {
  width: 100%;
  padding: 12px 0;
  text-align: center;
  color: white;
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  cursor: pointer;
  font-size: 11px;
  font-weight: 600;
  transition: all 0.2s ease;
}

.file-dropdown-item:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateX(2px);
}

.file-dropdown-item:last-child {
  border-bottom: none;
}

.sidebar-button,
.sidebar-button * {
  user-select: none;
}

/* Modified: Allow dragging for page buttons but prevent for other bottom bar elements */
#bottom-bar {
  user-select: none;
  -webkit-user-drag: none;
}

#bottom-bar *:not(.page-button) {
  user-select: none;
  -webkit-user-drag: none;
}

#corner-box,
#corner-box * {
  user-select: none;
  -webkit-user-drag: none;
}

#file-tab {
  user-select: none;
  -webkit-user-drag: none;
}

#page-label {
  user-select: none;
  -webkit-user-drag: none;
}

.page-button.drag-over {
  border: 2px dashed rgba(255, 255, 255, 0.6);
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 0 25px rgba(102, 126, 234, 0.6);
}

#actual-canvas {
  position: relative;
}

.resize-bar {
  position: absolute;
  background: transparent !important;
  z-index: 1000;
  opacity: 0; /* full invisibility */
}

.resize-top, .resize-bottom {
  height: 6px;
  width: 100%;
  left: 0;
  cursor: ns-resize;
}
.resize-top { top: -3px; }
.resize-bottom { bottom: -3px; }

.resize-left, .resize-right {
  width: 6px;
  height: 100%;
  top: 0;
  cursor: ew-resize;
}
.resize-left { left: -3px; }
.resize-right { right: -3px; }

body[data-has-cursor-image="true"].in-preview-mode * {
  cursor: none !important;
}

#scroll-buttons {
  overflow-y: auto;
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  padding-top: 10px;
}

.guide-line {
  position: absolute;
  background: #667eea;
  z-index: 9999;
  pointer-events: none;
  box-shadow: 0 0 8px rgba(102, 126, 234, 0.5);
}

.guide-line.h {
  position: absolute;
  left: 0;
  width: 100%;
  height: 0;
  border-top: 2px dashed #667eea;
  z-index: 9999;
}

.guide-line.v {
  position: absolute;
  top: 0;
  height: 100%;
  width: 0;
  border-left: 2px dashed #667eea;
  z-index: 9999;
}


#image-visuals-popup {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
  padding: 25px;
  border-radius: 20px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  z-index: 4000;
  display: none;
  flex-direction: column;
  gap: 20px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  width: 800px;
  max-width: 95vw;
  max-height: 90vh;
  overflow-y: auto;
  border: 1px solid rgba(0, 0, 0, 0.05);
  backdrop-filter: blur(10px);
}

.visuals-section {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.visuals-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 15px;
}

.visual-control {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.visual-control label {
  font-weight: 600;
  color: #2d3748;
  font-size: 14px;
}

.visual-control input[type="range"] {
  width: 100%;
  height: 6px;
  border-radius: 3px;
  background: #e2e8f0;
  outline: none;
  -webkit-appearance: none;
}

.visual-control input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
}

.visual-control input[type="color"] {
  width: 50px;
  height: 30px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
}

.tools-section {
  border-top: 1px solid #e2e8f0;
  padding-top: 20px;
}

.tools-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 10px;
}

.tool-button {
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
  color: white;
  border: none;
  border-radius: 10px;
  padding: 12px;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: center;
}

.tool-button:hover {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: translateY(-1px);
}

.tool-button.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 0 15px rgba(102, 126, 234, 0.4);
}

.tool-controls {
  display: flex;
  gap: 15px;
  align-items: center;
  margin-top: 15px;
}

.popup-title {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-size: 20px;
  font-weight: 700;
  padding: 15px 25px;
  border-radius: 12px;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  text-align: center;
}

.popup-buttons {
  display: flex;
  gap: 10px;
  justify-content: flex-end;
  border-top: 1px solid #e2e8f0;
  padding-top: 20px;
}

.popup-btn {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 10px;
  padding: 12px 24px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
}

.popup-btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
}

.popup-btn.cancel {
  background: linear-gradient(135deg, #718096 0%, #4a5568 100%);
}

#reset-view-button {
  position: fixed;
  bottom: 120px; /* Above the bottom bar */
  right: 20px;
  width: 50px;
  height: 50px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 12px;
  display: flex;
  justify-content: center;
  align-items: center;
  color: white;
  font-size: 20px;
  font-weight: bold;
  cursor: pointer;
  border: 1px solid rgba(255, 255, 255, 0.2);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 10;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
}

#reset-view-button:hover {
  background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
  transform: scale(1.05);
  box-shadow: 0 8px 25px rgba(102, 126, 234, 0.5);
}

#zoom-indicator {
  position: fixed;
  top: 20px;
  right: 20px;
  background: rgba(0, 0, 0, 0.7);
  color: white;
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 500;
  z-index: 100;
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
}

</style>

<!-- MAIN INTERFACE -->
<div id="left-box">
<div id="file-tab" onclick="toggleSlidingFileMenu()">
<img src="Images/TheroSpyIcon.png" alt="File" style="height: 90px; margin-top: 15px;">
</div>
<div id="scroll-buttons">
<div id="page-label">PAGE 1</div>
<div class="sidebar-button" onclick="addBackground()">Add Background</div>
<div class="sidebar-button" onclick="addImage()">Add Image/Video</div>
<div class="sidebar-button" onclick="showTextboxPopup()">Add Shape</div>
<div class="sidebar-button" onclick="showButtonPopup()">GUI Editor</div>
<div class="sidebar-button" onclick="addMusic()">Add Audio</div>
<div class="sidebar-button" onclick="openInteractionConfig()">Effects Editor</div>
<div class="sidebar-button" onclick="CursorSettings()">Cursor Editor</div>
</div>
</div>

<!-- NEW SLIDING FILE MENU -->
<div id="file-menu-overlay" onclick="closeSlidingFileMenu()"></div>
<div id="file-sliding-menu">
<button class="file-menu-item" onclick="event.stopPropagation(); saveAs()">Save As</button>
<button class="file-menu-item" onclick="event.stopPropagation(); saveFile()">Save</button>
<button class="file-menu-item" onclick="event.stopPropagation(); uploadFile()">Upload</button>
<button class="file-menu-item" onclick="event.stopPropagation(); openFile()">Open</button>
<button class="file-menu-item" onclick="event.stopPropagation(); shareFile()">Share</button>
<button class="file-menu-item" onclick="event.stopPropagation(); ExportFile()">Export</button>
<button class="file-menu-item" onclick="event.stopPropagation(); ImportFile()">Import</button>
<button class="file-menu-item" onclick="event.stopPropagation(); viewCode()">View Code</button>
<button class="file-menu-item" onclick="event.stopPropagation(); ToolsFile()">Tools</button>
<button class="file-menu-item" onclick="event.stopPropagation(); openHelpPopup()">Help</button>
<button class="file-menu-item" onclick="event.stopPropagation(); previewGame()">Preview Game</button>
</div>

<div id="scene-area">
<div id="canvas-wrapper">
<div id="actual-canvas"></div>
</div>
</div>

<div id="bottom-bar">
<div id="page-buttons"></div>
</div>

<div id="trash-can">🗑️</div>

<div id="reset-view-button" onclick="resetCanvasView()" title="Reset View">⌂</div>
<div id="zoom-indicator">100%</div>

<div id="corner-box">
<div id="corner-button" onclick="openGlobalProcedures()">
<img src="Images/GlobalIcon.png" alt="Global Icon" style="width: 20px; height: 20px; vertical-align: middle; margin-right: 6px;">Global</div>
</div>


<script src="https://cdn.jsdelivr.net/npm/jszip@3.10.0/dist/jszip.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/file-saver@2.0.5/dist/FileSaver.min.js"></script>
<script src="Scripts/textboxPopup.js"></script>
<script src="Scripts/addImage.js"></script>
<script src="Scripts/PreviewGame.js"></script>
<script src="Scripts/helpPopup.js"></script>
<script src="Scripts/clickableButton.js"></script>
<script src="Scripts/addBackground.js"></script>
<script src="Scripts/page.js"></script>
<script src="Scripts/viewCode.js"></script>
<script src="Scripts/openInteractionConfig.js"></script>
<script src="Scripts/addMusic.js"></script>
<script src="Scripts/ImagePreviewFunctions.js"></script>
<script src="Scripts/fileuploadsave.js"></script>
<script src="Scripts/cursorsettings.js"></script>
<script src="Scripts/CanvasFunctions.js"></script>
<script src="https://unpkg.com/blockly/blockly.min.js"></script>


<script>

//
//
//
//
// Variables
//
//
//
//

let isFileMenuOpen = false;
let currentPage = 1;
let totalPages = 1;
let pages = { 1: [] };
let selectedPageForDelete = null;
let copiedButtonLayout = null;
let savedTextboxLayout = null;
if (!pages[currentPage]) pages[currentPage] = [];
if (!pages[currentPage].meta) pages[currentPage].meta = {};
let inPreviewMode = false;
let justInteracted = false;
let remainingAssignedImages = (pages[currentPage]?.meta?.textboxConfig?.assignedNames || []).length;
let actionHistory = [];
let redoHistory = [];
let maxHistorySize = 20;
let savedEventListeners = [];
let globalImages = [];
let currentEditingImage = null;
let currentTool = 'select';
let originalImageFilters = {};


//
//
//
//
//
// Functions
//
//
//
//

function toggleSlidingFileMenu() {
  const menu = document.getElementById('file-sliding-menu');
  const overlay = document.getElementById('file-menu-overlay');

  if (isFileMenuOpen) {
    closeSlidingFileMenu();
  } else {
    openSlidingFileMenu();
  }
}

function openSlidingFileMenu() {
  const menu = document.getElementById('file-sliding-menu');
  const overlay = document.getElementById('file-menu-overlay');

  menu.classList.add('open');
  overlay.classList.add('active');
  isFileMenuOpen = true;
}

function closeSlidingFileMenu() {
  const menu = document.getElementById('file-sliding-menu');
  const overlay = document.getElementById('file-menu-overlay');

  menu.classList.remove('open');
  overlay.classList.remove('active');
  isFileMenuOpen = false;
}

function saveAs() {
  alert('Save As clicked');
  closeSlidingFileMenu();
}

function saveFile() {
  alert('Save clicked');
  closeSlidingFileMenu();
}

function uploadFile() {
  alert('Upload clicked');
  closeSlidingFileMenu();
}

function openFile() {
  alert('Open clicked');
  closeSlidingFileMenu();
}

function shareFile() {
  alert('Share clicked');
  closeSlidingFileMenu();
}

function saveState() {
  const state = {
    canvas: document.getElementById("actual-canvas").innerHTML,
    pages: JSON.parse(JSON.stringify(pages)),
    currentPage: currentPage,
    totalPages: totalPages
  };

  actionHistory.push(state);
  redoHistory = [];

  if (actionHistory.length > maxHistorySize) {
    actionHistory.shift();
  }
}

function undoLastAction() {
  if (actionHistory.length === 0) return;

  const currentState = {
    canvas: document.getElementById("actual-canvas").innerHTML,
    pages: JSON.parse(JSON.stringify(pages)),
    currentPage: currentPage,
    totalPages: totalPages
  };
  redoHistory.push(currentState);

  const canvas = document.getElementById("actual-canvas");
  const previousState = actionHistory.pop();

  canvas.innerHTML = previousState.canvas;
  pages = previousState.pages;
  currentPage = previousState.currentPage;
  totalPages = previousState.totalPages;

  selectedElement = null;
  removeResizeBars();

  rebindElementEvents();
  renderPageButtons();
  updatePageLabel();
}

function redoLastAction() {
  if (redoHistory.length === 0) return;

  const currentState = {
    canvas: document.getElementById("actual-canvas").innerHTML,
    pages: JSON.parse(JSON.stringify(pages)),
    currentPage: currentPage,
    totalPages: totalPages
  };
  actionHistory.push(currentState);

  const canvas = document.getElementById("actual-canvas");
  const nextState = redoHistory.pop();

  canvas.innerHTML = nextState.canvas;
  pages = nextState.pages;
  currentPage = nextState.currentPage;
  totalPages = nextState.totalPages;

  selectedElement = null;
  removeResizeBars();

  rebindElementEvents();
  renderPageButtons();
  updatePageLabel();
}

function isClickOnOpaquePixel(img, event, callback) {
  if (!img.complete || img.naturalWidth === 0) {
    img.onload = () => isClickOnOpaquePixel(img, event, callback);
    return;
  }

  const rect = img.getBoundingClientRect();
  const x = event.clientX - rect.left;
  const y = event.clientY - rect.top;

  const scaleX = img.naturalWidth / rect.width;
  const scaleY = img.naturalHeight / rect.height;
  const pixelX = Math.floor(x * scaleX);
  const pixelY = Math.floor(y * scaleY);

  const canvas = document.createElement('canvas');
  canvas.width = img.naturalWidth;
  canvas.height = img.naturalHeight;
  const ctx = canvas.getContext('2d');
  ctx.drawImage(img, 0, 0);

  try {
    const alpha = ctx.getImageData(pixelX, pixelY, 1, 1).data[3];
    if (alpha > 0) callback();
  } catch (err) {
    console.warn('Pixel check failed:', err);
    callback();
  }
}

function rebindElementEvents() {
  document.querySelectorAll('#actual-canvas img.selectable').forEach(img => {
    img.replaceWith(img.cloneNode(true));
    const newImg = document.querySelector(`#actual-canvas img[src="${img.src}"]`);

    newImg.addEventListener("dblclick", function (e) {
      if (this.classList.contains("selectable")) {
        openNamePopupForImage(this);
      }
    });

    newImg.addEventListener("click", function (e) {
      if (!inPreviewMode && !justInteracted) {
        newImg.addEventListener("contextmenu", function (e) {
          e.preventDefault();
          if (!inPreviewMode) {
            openImageVisualsPopup(this);
          }
        });

        if (this.style.clipPath || this.dataset.hasTransparency === 'true') {
          isClickOnOpaquePixel(this, e, () => {
            document.querySelectorAll(".selected").forEach(el => el.classList.remove("selected"));
            removeResizeBars();
            selectedElement = this;
            this.classList.add("selected");
            addResizeBars(this);
            e.stopPropagation();
          });
        } else {
          document.querySelectorAll(".selected").forEach(el => el.classList.remove("selected"));
          removeResizeBars();
          selectedElement = this;
          this.classList.add("selected");
          addResizeBars(this);
          e.stopPropagation();
        }
      }
    });
  });

  document.querySelectorAll('#actual-canvas .selectable[data-box-type="textbox"]').forEach(textbox => {
    textbox.addEventListener("dblclick", function (e) {
      if (this.classList.contains("selectable") && this.dataset.boxType === 'textbox') {
        openTextboxEditorGUI(this);
      }
    });
  });

  document.querySelectorAll('#actual-canvas .selectable-button').forEach(button => {
    // Add any specific button event handlers here
  });
}




//
//
//
//
// Listeners
//
//
//
//


document.addEventListener("keydown", (e) => {
  if (e.ctrlKey && e.shiftKey && e.key.toLowerCase() === "z") {
    e.preventDefault();
    redoLastAction();
  } else if (e.ctrlKey && e.key.toLowerCase() === "z") {
    e.preventDefault();
    undoLastAction();
  }
});

document.getElementById("actual-canvas").addEventListener("dblclick", function (e) {
  if (e.target.tagName === "IMG" && e.target.classList.contains("selectable")) {
    openNamePopupForImage(e.target);
  }
});

document.getElementById("actual-canvas").addEventListener("dblclick", function (e) {
  if (e.target.classList.contains("selectable") && e.target.dataset.boxType === 'textbox') {
    openTextboxEditorGUI(e.target);
  }
});


document.addEventListener("keydown", (e) => {
  if (e.key.toLowerCase() === "escape") {
    exitPreview();
  }
});


document.addEventListener('keydown', (e) => {
  if (e.ctrlKey && e.key.toLowerCase() === 'c') {
    if (selectedElement) {
      copiedElement = selectedElement.cloneNode(true);
      copiedElement.classList.remove('selected');
    }
  }

  if (e.ctrlKey && e.key.toLowerCase() === 'v') {
    if (copiedElement && document.getElementById('actual-canvas')) {
      saveState();

      const newElement = copiedElement.cloneNode(true);
      newElement.style.left = (parseFloat(copiedElement.style.left || "0") + 2) + "%";
      newElement.style.top = (parseFloat(copiedElement.style.top || "0") + 2) + "%";
      document.getElementById('actual-canvas').appendChild(newElement);
      pages[currentPage].push(newElement);
    }
  }
});

renderPageButtons();
loadScene(currentPage);
updatePageLabel();
setupTrashCan();


// Close menu when pressing Escape key
document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape' && isFileMenuOpen) {
    closeSlidingFileMenu();
  }
});


</script>